# 功能测试文档 - 内容类型选择器

## 新增功能
在设置页面添加了"显示位置"选项，允许用户选择在哪些内容类型中显示目录。

## 测试步骤

### 1. 检查设置页面
1. 进入 WordPress 后台
2. 访问 `设置` → `Elegant TOC`
3. 在"基本设置"区块中，应该能看到新的"显示位置"选项
4. 该选项应该列出所有公开的文章类型（post、page 及自定义类型）
5. 默认情况下，"文章"和"页面"应该被勾选

### 2. 测试保存功能
1. 取消勾选"页面"
2. 点击"保存设置"
3. 刷新页面，确认"页面"保持未勾选状态
4. 访问任意页面，确认目录不再显示
5. 访问任意文章，确认目录仍然显示

### 3. 测试默认值
1. 取消所有内容类型的勾选
2. 点击"保存设置"
3. 刷新页面，应该看到"文章"和"页面"重新被勾选（自动回退到默认值）

### 4. 测试自定义文章类型
如果你的网站有自定义文章类型（如 Portfolio、Product 等）：
1. 在设置页面应该能看到这些自定义类型
2. 勾选某个自定义类型
3. 保存设置
4. 访问该自定义类型的文章，确认目录显示

### 5. 测试过滤器兼容性
如果你在主题或其他插件中使用了 `elegant_toc_post_types` 过滤器：
1. 过滤器应该仍然有效
2. 过滤器的结果会基于用户在设置页面的选择

## 预期结果

### 后端设置页面
- ✅ 显示所有公开的文章类型（排除附件）
- ✅ 每个类型显示友好名称和技术名称，如："文章 (post)"
- ✅ 默认勾选"文章"和"页面"
- ✅ 保存后正确记住用户的选择

### 前端显示逻辑
- ✅ 只在用户勾选的内容类型中显示目录
- ✅ 未勾选的内容类型不显示目录
- ✅ 向后兼容：升级后保持原有行为（文章和页面都显示）

### 安全性
- ✅ 输入验证：只接受有效的公开文章类型
- ✅ 自动排除附件类型
- ✅ 空值回退：未选择任何类型时自动使用默认值

## 代码变更摘要

### 修改的文件
1. `admin/settings-page.php` - 添加内容类型选择器 UI
2. `elegant-toc.php` - 更新选项处理逻辑

### 关键变更
- 添加 `post_types` 选项字段
- 动态获取所有公开的文章类型
- 在 `get_supported_post_types()` 中读取用户选择
- 在 `sanitize_options()` 中验证用户输入
- 保持 `elegant_toc_post_types` 过滤器的兼容性
